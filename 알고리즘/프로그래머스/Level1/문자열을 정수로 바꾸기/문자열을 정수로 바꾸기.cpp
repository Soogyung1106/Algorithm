#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    /*
    if(s[0] == '+'){
        return stoi(s.substr(1, s.size()));
    }else if(s[0] == '-'){
        
    }else{
        return stoi(s);
    }
    */
    return stoi(s);
    
}